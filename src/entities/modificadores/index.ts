import { Atributos } from "../atributos";
import Modificador from "./model/Modificador";

export const modificadores = [
  new Modificador(Atributos.FORCA, 0, 0, 0),
  new Modificador(Atributos.DESTREZA, 0, 0, 0),
  new Modificador(Atributos.CONSTITUICAO, 0, 0, 0),
  new Modificador(Atributos.INTELIGENCIA, 0, 0, 0),
  new Modificador(Atributos.SABEDORIA, 0, 0, 0),
  new Modificador(Atributos.CARISMA, 0, 0, 0),
];
